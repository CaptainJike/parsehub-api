# Handoff Checklist

- Read `AGENTS.md`.
- Confirm changed files are inside `E:\github\parsehub-api`.
- Run `npm test` for logic changes.
- Run `npm run build` for TypeScript, Prisma, or dependency changes.
- Update docs for public API or workflow changes.
- Summarize user-visible behavior and verification results.
- Call out unfinished production integrations, especially WeChat Pay signing.
